{
  "name": "axis-discovery-bonjour-sample",
  "version": "1.0.0",
  "private": true,
  "main": "sample.js",
  "scripts": {
    "clean": "rimraf \"./**/*.+(d.ts|js|js.map)\"",
    "prebuild": "yarn clean",
    "build": "tsc",
    "test": "echo noop",
    "lint": "echo noop",
    "prestart": "yarn build",
    "start": "node ./sample.js"
  },
  "dependencies": {
    "axis-discovery-bonjour": "2.0.0"
  },
  "devDependencies": {
    "@types/node": "14.14.39",
    "rimraf": "3.0.2",
    "typescript": "4.2.4"
  }
}
