{
  "name": "axis-configuration-sample",
  "version": "1.0.0",
  "private": true,
  "main": "sample.js",
  "scripts": {
    "clean": "rimraf \"./**/*.+(d.ts|js|js.map)\"",
    "prebuild": "yarn clean",
    "build": "tsc",
    "test": "echo noop",
    "lint": "echo noop",
    "prestart": "yarn build",
    "start": "node ./sample.js"
  },
  "dependencies": {
    "axis-configuration": "2.1.2"
  },
  "devDependencies": {
    "@types/node": "14.14.6",
    "rimraf": "3.0.2",
    "typescript": "4.0.5"
  }
}
